// Theme type
const SET_LIGHT_THEME_TYPE = 'SET_LIGHT_THEME_TYPE';
const RESET_THEME_TYPE = 'RESET_THEME_TYPE';

const CommomActionTypes = {
  SET_LIGHT_THEME_TYPE,
  RESET_THEME_TYPE,
};

export default CommomActionTypes;
